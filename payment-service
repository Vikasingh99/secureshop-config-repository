server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/payment_db
    username: postgres
    password: admin1234

    hikari:
      minimum-idle: 2
      maximum-pool-size: 10

app:
  service-name: payment service
  message: payment configuration loaded from GitHub
